% ANN_UAVCtrl_BlockDiagram.tex
\tikzset{%
  every neuron/.style={
    circle,
    draw,
    minimum size=1cm,
		line width=1.0mm
  },
  neuron missing/.style={
    draw=none, 
    scale=4,
    text height=0.333cm,
    execute at begin node=\color{black}$\vdots$
  },
}
\tikzstyle{ArrowObject1}=[line width=1.5mm, -latex]
\tikzstyle{ArrowObject2}=[line width=1.5mm, latex-]
\resizebox{!}{0.4\textwidth}{
	\begin{tikzpicture}[x=1.5cm, y=1.5cm, >=stealth]
		% Input layer nodes
		\foreach \m/\l [count=\y] in {1,2,3,4}
			\node [every neuron/.try, neuron \m/.try] (input-\m) at (0,2.5-\y) {};
		% Hidden layer nodes
		\foreach \m [count=\y] in {1,2,3,missing,4}
			\node [every neuron/.try, neuron \m/.try ] (hidden-\m) at (3,4.0-\y*1.10) {};
		% Output layer nodes
		%\foreach \m [count=\y] in {1,2,missing,3}
			\node [every neuron/.try, neuron \m/.try ] (output-\m) at (6,3.0-\y) {};

		% Input layer labels
		%\foreach \l [count=\i] in {1,2,3}
			\draw [ArrowObject2] (input-1) -- ++(-3.5,0)
				node [above, midway] {Inertial};
			\draw [ArrowObject2] (input-2) -- ++(-3.5,0)
				node [above, midway] {Pressure};
			\draw [ArrowObject2] (input-3) -- ++(-3.5,0)
				node [above, midway] {Strain};
		% Hidden layer labels
		%\foreach \l [count=\i] in {1,2,3,n}
			%\node [above] at (hidden-\i.north) {$H_\l$};
		% Output layer labels
		%\foreach \l [count=\i] in {1,2,n}
			\draw [ArrowObject1] (output-1) -- ++(2,0)
				node [above, midway] {$\delta_{ail}$};
			\draw [ArrowObject1] (output-2) -- ++(2,0)
				node [above, midway] {$\delta_{ele}$};
			\draw [ArrowObject1] (output-3) -- ++(2,0)
				node [above, midway] {$\delta_{n}$};
				
		% Input layer connecting arrows
		\foreach \i in {1,...,3}
			\foreach \j in {1,...,4}
				\draw [ArrowObject1] (input-\i) -- (hidden-\j);
		% Hidden layer connecting arrows
		\foreach \i in {1,...,4}
			\foreach \j in {1,...,3}
				\draw [ArrowObject1] (hidden-\i) -- (output-\j);

		%\foreach \l [count=\x from 0] in {Input, Hidden, Output}
			%\node [align=center, above] at (0,4.5) {Input\\layer};
			%\node [align=center, above] at (3,4.5) {Hidden\\layers};
			%\node [align=center, above] at (6,4.0) {Control\\Output\\layer};

	\end{tikzpicture}
}